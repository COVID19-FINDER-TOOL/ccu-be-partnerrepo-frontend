import React from "react";

const litrals = {
    buttons : {
        loginButton:{
                buttonText : "Login",
                variant:"primary",
        }
    }
};

export default litrals;